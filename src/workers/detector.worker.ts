/**
 * Web Worker stub — kept for compatibility.
 *
 * Card detection now runs on the main thread using pure computer vision
 * (no TensorFlow.js or ML models). This worker is no longer used.
 */

export {}
